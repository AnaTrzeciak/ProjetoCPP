Capitulo 4 Computation:

**contextpr: significa expressão constante. Não pode ser mudada no programa.
**const: declara um objeto como uma constante.

Operadores:

- &&: significa "e";
- ||: significa "ou";
  
Conversões:

-type(value) ou type{value}: significa converta valor para o tipo. Ex: int(5)

Declarações:

- sintaxe if-else:

 (i) if(condição) afirmação else afirmação
 
 (ii) if(condição) afirmação else if(condição) afirmação else afirmação

- sintaxe switch:
 
  switch(caso){
    case 'variavel(não pode ser string)':
	afirmação;
    	break;
    case 'variavel':
	afirmação;
	break;
    default:
	afirmação;
	break;
	}

